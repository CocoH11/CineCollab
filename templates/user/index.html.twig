{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}
    <h1>Mon compte</h1>

    <div class="row">
        <div class="col-2">Pseudonyme</div>
        <div class="col-2">{{ user.username }}</div>
    </div>
    <div class="row">
        <div class="col-2">Prénom</div>
        <div class="col-2">{{ user.prenom }}</div>
    </div>
    <div class="row">
        <div class="col-2">Nom</div>
        <div class="col-2">{{ user.nom }}</div>
    </div>
    <div class="row">
        <div class="col-2">Photo</div>
        <div class="col-2">
            {% if(user.photo) %}
            <img src="{{ asset(user.photo) }}" alt="Photo de profil" width="150px" class="img-fluid">
            {% else %}
            <img src="{{ asset('default_imageProfil.png') }}" alt="Photo de profil" width="150px" class="img-fluid">
            {% endif %}
        </div>
    </div>
    <div class="row">
        <div class="col-2"></div>
        <div class="col-2"></div>
    </div>
{% endblock %}